Status: Late-alert email pipeline scaffolded; awaiting SMTP credentials for live sends.

Completed:
- Client triggers `/api/share/app/mch/late-alert` once por usuario/semana cuando excede el límite.
- Backend expone endpoint con validaciones y delega en `NotificationService`.
- `NotificationService` con Nodemailer y caché anti-duplicado listo para credenciales.
- Documentación/env placeholders añadidos y dependencias actualizadas.
